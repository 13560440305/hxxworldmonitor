/**
 * Map popup on-demand translation (stub — wire to translateText later).
 */
export async function translateMapPopupContent(
  _text: string,
  _targetLang: string,
): Promise<string | null> {
  // TODO: return translateText(text, targetLang) from summarization.ts
  return null;
}
